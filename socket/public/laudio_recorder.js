(function(window){
  var Laudio = function(){
    
  };

  /**
   * upload a audioBuffer
   * @param {String} id 
   * @param {Buffer} audioBuffer 
   */
  Laudio.upload = function(id,audioBuffer){
    
  };

  /**
   * finish a audio
   * @param {String} id 
   */
  Laudio.stop = function(id){

  };

  Laudio.haveCallback = function(){

  };
  window.Recorder = Recorder;
})(window);